on_actions = {
    on_startup = {
        effect = {
	every_other_country = {
 		    limit = {
     		     	is_ai = no
    		 }
 		    set_rule = { can_send_volunteers = yes }
			set_rule = { can_create_factions = yes }
			}
    	 }
	}
}